git-semver
==========

`git-semver` is a tiny and simple tool that just dumps the current or future version number of your project, for use
in conjunction with git tags to manage a project's releases.

